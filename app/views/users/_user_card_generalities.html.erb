<div class="identity">
    <div>
        <%= cl_image_tag(user.profile.avatar.key, radius: :max, height: 50, width: 50, crop: :scale, border: "2px_solid_darkgreen", format: :png) %>
    </div>
    <div>
        <%= link_to "#{user.name}", user_path(user.id) %>
    </div>
</div>
<div class="followings">
    <div id="follows-count-<%= section %>-<%= user.id %>">
        <% if user.follows.count == 0 %>
            Stalkings: <%= user.follows.count %>
        <% else %>
            Stalkings: <%= link_to "#{user.follows.count}", user_followings_path(user, type: "Follows") %>
        <% end %>
    </div>
    <div id="followers-count-<%= section %>-<%= user.id %>">
        <% if user.followers.count == 0 %>
            Stalkers: <%= user.followers.count %>
        <% else %>
            Stalkers: <%= link_to "#{user.followers.count}", user_followings_path(user, type: "Followers") %>
        <% end %>
    </div>
</div>