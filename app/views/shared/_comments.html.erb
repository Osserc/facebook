<li>
  <%= comments.body %> -

  <%= form_with model: Comment.new do | form | %>
    <%= form.hidden_field :commentable_type, value: comments.model_name.human %>
    <%= form.hidden_field :commentable_id, value: comments.id %>
    <%= form.text_area :body, placeholder: "Add a comment" %><br/>
    <%= form.submit "Comment"  %>
    <% end %>

  <ul>
      <%= render partial: 'shared/comments', collection: comments.comments %>
  </ul>

</li>