<% if follows?(user) %>
    <%= button_to "Unstalk", following_path(find_follower(user).id), method: :delete, params: { user_id: user.id } %>
<% else %>
    <%= button_to "Stalk", followings_path, params: { user_id: user.id } %>
<% end %>