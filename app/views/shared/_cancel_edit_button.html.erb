<%= turbo_frame_tag dom_id(post, "edit") do %>
    <% if same_user?(post.author) %>
        <%= button_to "Close", user_post_path(current_user), method: :get, data: { turbo_frame: "#{post.model_name.human.downcase}_#{post.id}" } %>
    <% end %>
<% end %>

<%# render turbo_stream: turbo_stream.update("edit_#{@post.model_name.human.downcase}_#{@post.id}", partial: "shared/cancel_edit_button", locals: { post: @post }) %>
