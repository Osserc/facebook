<%= turbo_stream.update "nested-list-#{@comment.model_name.human.downcase}-#{@comment.id}" %>

<%= turbo_stream.update "show-children-#{@comment.id}" do %>
    <%= button_to "Show more", more_comment_path(@comment), params: { commentable_type: @comment.model_name.human, commentable_id: @comment.id } %>
<% end %>